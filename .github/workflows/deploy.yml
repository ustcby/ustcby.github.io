name: Deploy site

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2.2"
          bundler-cache: true
      - name: Build with Jekyll (debug)
        run: |
          bundle exec jekyll build --trace 2>&1 | tee build.log || true
          echo "=== EXIT CODE: $? ==="
          echo "=== _site exists? ==="
          ls -la _site/ 2>/dev/null | head -30 || echo "NO _site DIR"
          echo "=== _site/assets/css ==="
          ls -la _site/assets/css/ 2>/dev/null || echo "no css dir"
          echo "=== build.log tail 80 ==="
          tail -80 build.log
          echo "=== build.log grep -i error ==="
          grep -in "error\|exception\|fatal\|failed\|cannot\|undefined" build.log | head -30 || echo "no error matches"
          exit 1
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
